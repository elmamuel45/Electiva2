name: Deploy to Surge

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Build and Deploy
      env:
        SURGE_EMAIL: manueljesusmedinacepeda@gmail.com  
        SURGE_PASSWORD: Manuel2134
      run: |
        # Configurar Surge.sh
        npm install --global surge
        surge login --email $SURGE_EMAIL --password $SURGE_PASSWORD

        # Desplegar en Surge.sh
        surge --project ./ --domain estudiante1234M.surge.sh
