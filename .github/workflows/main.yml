name: Deploy to Surge

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Build and Deploy
      env:
        SURGE_EMAIL: tu_email
        SURGE_PASSWORD: tu_contraseña
      run: |
        # Configurar Surge.sh
        npm install --global surge
        surge login --email $manueljesusmedinacepeda@gmail.com --password $Manuel2134

        # Desplegar en Surge.sh
        surge --project ./ --domain estudiante1234M.surge.sh
