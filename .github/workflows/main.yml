name: Deploy to Surge

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Login to Surge.sh
      run: echo -e "manueljesusmedinacepeda@gmail.com\nManuel2134" | surge login

    - name: Build and Deploy
      run: |
        surge --project ./ --domain estudiante-m-n-s.surge.sh
